package Snippet;

public class JumpGame2 {
	
	public int minJumps(int [] nums){
		int []minJumps = new int[nums.length];
		int []fromTo = new int[nums.length];
		
		for(int i=1;i<nums.length;i++){
			int left = 0;
			while(left < i){
				if((left+nums[left]) >= i){
					int paths = minJumps[left]
				}
			}
		}
	}
}
